# Early humair publication acceptance

All 100 original MP3 files (428,744,519 bytes) passed anonymous independent
verification at dataset revision `782d3291a031f64afaf8052643a9644732300826`.
Public prefix: `audio/early_humair_originals_v2/` in
https://huggingface.co/datasets/EZMONYI/music-ai-human-test-audio

The verifier compared the public manifest exactly with the pinned recovered
records in `../early_humair_upstream_recovery_v2/records.json`, and checked all
100 public LFS SHA-256 values and sizes. It did not redownload the entire audio.
Both the attribution notice and source card were fetched and hashed. The one
UUID with multiple attribution variants remains explicitly represented.

`COMMIT.json` is the publisher receipt; `INDEPENDENT_ACCEPTANCE.json` is the
separate read-only verification receipt. Neither establishes whole-project
delivery completion or independent third-party rights clearance.
